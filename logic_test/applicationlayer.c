#include "applicationlayer.h"

int handleApplicationLayer(unsigned char *msg, size_t msg_size, unsigned char **p_payload, size_t *p_payload_size, unsigned char *buf, size_t *p_buf_size){
    if(msg_size < 1){
    return FAIL;
  }

  return FAIL;
}

